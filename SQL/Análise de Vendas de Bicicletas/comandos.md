Neste arquivo, escrevo todos os comandos utilizados no SQL para realizar a análise dos dados.

Primeiramente, começamos criando as nove tabelas relacionadas:

```
CREATE TABLE brands (
	brand_id INT PRIMARY KEY,
	brand_name VARCHAR(255)
);
```
